import { StyleSheet } from "react-native";

export const Style = StyleSheet.create({
    container: {
        padding: 0
    },
    addButton: {
        borderRadius: 99,
        width: 65,
        height: 65,
        position: 'absolute',
        bottom: 20,
        right: 30
      },
})