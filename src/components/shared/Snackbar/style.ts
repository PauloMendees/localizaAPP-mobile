import { StyleSheet } from "react-native";

export const Style = StyleSheet.create({
    alertContainer: {
        position: 'absolute',
        top: 40,
        width: '90%',
        borderRadius: 50,
        left: '5%',
        zIndex: 99
    }
})