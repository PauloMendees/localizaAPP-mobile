import { StyleSheet, Dimensions } from 'react-native'

const Style = StyleSheet.create({
    checkBoxContainer: {
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center'
    },

    checkBoxLabel: {
        fontSize: 12,
        opacity: 0.8
    }
})

export { Style }